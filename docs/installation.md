# Installation

JustFit is not published to PyPI yet. Install from source:

```bash
git clone https://github.com/YuhuaBillChen/justfit-mlx
cd justfit-mlx
pip install -e .
```

The import package is `mlx_vlm`, so existing mlx-vlm code keeps working
unchanged.

